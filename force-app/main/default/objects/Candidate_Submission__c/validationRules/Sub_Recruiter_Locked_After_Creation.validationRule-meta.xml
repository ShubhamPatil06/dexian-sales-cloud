<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Sub_Recruiter_Locked_After_Creation</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    $Profile.Name &lt;&gt; &quot;System Administrator&quot;,
    NOT(ISNEW()),
    OR(
        ISCHANGED( Submitting_Recruiter__c)
    )
)</errorConditionFormula>
    <errorMessage>Submitting Recruiter Cannot Be Changed Once Record is Created. Create a new Submission or Contact Your Administrator</errorMessage>
</ValidationRule>
