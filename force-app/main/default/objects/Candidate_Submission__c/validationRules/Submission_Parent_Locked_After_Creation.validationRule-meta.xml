<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Submission_Parent_Locked_After_Creation</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    $Profile.Name &lt;&gt; &quot;System Administrator&quot;,
    NOT(ISNEW()),
    OR(
        ISCHANGED(Candidate__c),
        ISCHANGED(Job_Order__c)
    )
)</errorConditionFormula>
    <errorMessage>&quot;Candidate and Job Order cannot be changed once a submission has been created. Create a new submission instead.&quot;</errorMessage>
</ValidationRule>
